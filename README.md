# Kinesis.js

CSS-native reactive motion for the web.

```bash
npm install @amineyarman/kinesis
```

```css
.card {
  --k-parallax: 40px 20px;
  --k-tilt: 8deg;
  --k-motion: soft;
}
```

```html
<section data-kinesis>
  <article class="card">...</article>
</section>
```
